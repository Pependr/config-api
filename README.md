<h1>ConfigAPI</h1>
<hr>
ToDo:
  - Object oriented registries + registries for srls and drls
  - File IO and management (json and tomlkit + pathlib) [ideas: context manager OR module OR class] + pytests
  - Integrations (pathlib, dataclasses, enum) + pytests
